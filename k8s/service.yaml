apiVersion: v1
kind: Service

metadata:
  name: employee-management-service

spec:
  type: NodePort

  selector:
    app: employee-management

  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      nodePort: 30001
